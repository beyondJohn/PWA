export class Hide {
    public userNumber: string;
    public showcase: string;

    constructor(
        userNumber: string
        , showcase: string
    ) {
        this.userNumber = userNumber;
        this.showcase = showcase;

    }
}